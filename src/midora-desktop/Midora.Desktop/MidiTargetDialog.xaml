<Window x:Class="Midora.Desktop.MidiTargetDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:ui="clr-namespace:Midora.Desktop.Presentation.Controls;assembly=Midora.Desktop.Presentation"
        Title="Select MIDI Target" Width="500" Height="340" MinHeight="340" ResizeMode="NoResize"
        WindowStyle="None" WindowStartupLocation="CenterOwner" Background="{StaticResource Brush.Surface.0}"
        SnapsToDevicePixels="True" UseLayoutRounding="True">
  <shell:WindowChrome.WindowChrome><shell:WindowChrome CaptionHeight="38" CornerRadius="0" GlassFrameThickness="0" ResizeBorderThickness="0" UseAeroCaptionButtons="False"/></shell:WindowChrome.WindowChrome>
  <Border BorderBrush="{StaticResource Brush.Border.Strong}" BorderThickness="1"><Grid>
    <Grid.RowDefinitions><RowDefinition Height="38"/><RowDefinition Height="*"/><RowDefinition Height="58"/></Grid.RowDefinitions>
    <Grid Background="{StaticResource Brush.Black}" MouseLeftButtonDown="OnTitleMouseDown"><TextBlock x:Name="DialogTitleText" Margin="12,0" VerticalAlignment="Center" FontWeight="SemiBold" Text="Select MIDI Target"/><Button Width="46" Height="38" HorizontalAlignment="Right" Style="{StaticResource Button.Caption.Close}" Click="OnCancelClick"><ui:FluentIcon Style="{StaticResource Icon.WindowControl}" Data="{StaticResource WindowControl.Close}"/></Button></Grid>
    <StackPanel Grid.Row="1" Margin="18">
      <TextBlock Style="{StaticResource Text.Caption}" Text="Select a non-Note MIDI or advanced value target. CC91 and CC93 are rejected by the initial-release profile." TextWrapping="Wrap"/>
      <TextBlock Margin="0,14,0,4" Style="{StaticResource Text.Overline}" Text="TARGET KIND"/>
      <ComboBox x:Name="KindBox" SelectionChanged="OnKindChanged"/>
      <TextBlock x:Name="NumberLabel" Margin="0,12,0,4" Style="{StaticResource Text.Overline}" Text="CONTROLLER"/>
      <ComboBox x:Name="ControllerBox" DisplayMemberPath="DisplayName"/>
      <TextBox x:Name="NumberBox" Visibility="Collapsed" Text="0"/>
      <TextBlock x:Name="ValidationText" Margin="0,10,0,0" Foreground="{StaticResource Brush.Red.Hover}" TextWrapping="Wrap"/>
    </StackPanel>
    <Border Grid.Row="2" Padding="10" Background="{StaticResource Brush.Surface.1}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="0,1,0,0"><StackPanel HorizontalAlignment="Right" Orientation="Horizontal"><Button Margin="0,0,7,0" Style="{StaticResource Button.Dialog.Cancel}" Content="Cancel" Click="OnCancelClick"/><Button Style="{StaticResource Button.Dialog.Confirm}" Content="Select" Click="OnSelectClick"/></StackPanel></Border>
  </Grid></Border>
</Window>
